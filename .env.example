# Email SMTP configuration for contact form
# Copy this file to .env and fill in the values

# SMTP account used to send the email (Outlook/Hotmail example)
SMTP_USER=your_hotmail_or_outlook_email@example.com
SMTP_PASS=your_email_password_or_app_password

# Optional overrides (defaults are set for Outlook/Hotmail)
SMTP_HOST=smtp.office365.com
SMTP_PORT=587
SMTP_FROM=${SMTP_USER}

# Target recipient (defaults to Elvis's email if not set)
TARGET_EMAIL=Elvis-mujica@hotmail.com

# Server port
PORT=3001